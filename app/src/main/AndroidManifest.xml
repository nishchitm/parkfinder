<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="evolveres.in.parkfinder">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name="android.support.multidex.MultiDexApplication">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".splash.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainMap"
            android:label="@string/title_activity_main">
        </activity>
        <activity android:name=".slider.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".user_vendor.vendor_map"
            android:label="@string/title_activity_vendor_map">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="evolveres.in.parkfinder.user_vendor.vendor_activity" />
        </activity>
        <activity
            android:name=".user_client.client_map"
            android:label="@string/title_activity_client_map">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="evolveres.in.parkfinder.user_client.client_activity" />
        </activity>
        <activity android:name=".loginandregister.LoginActivity"
            android:label="@string/app_name"/>
        <activity android:name=".loginandregister.RegisterActivity"
            android:label="@string/app_name"/>
        <activity android:name=".user_client.client_activity"
            android:label="@string/app_name"/>
        <activity android:name=".user_vendor.vendor_activity"
            android:label="@string/app_name"/>
        <activity android:name=".About_us"
            android:label="@string/app_name"/>
        <activity android:name=".User_Details"
            android:label="@string/app_name"/>


    </application>

</manifest>